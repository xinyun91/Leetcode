public class Solution {
    public int removeElement(int[] A, int elem) {
        int n=A.length;
        int j=0;
        for(int i=0;i<A.length;i++){
            if(A[i]!=elem) A[j++]=A[i]; 
            else n--;
        }
        return n;
    }
}
